{
    "scripts": {
        "build": "tsc --pretty -p . && NODE_PATH=. `npm config get prefix`/bin/browserify CYOAE.js > main.js"
    },
    "dependencies": {},
    "devDependencies": {
        "@types/node": "^14.14.14"
    }
}
